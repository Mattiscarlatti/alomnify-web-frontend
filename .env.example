# Frontend only needs Stripe publishable key
# Backend server handles all other services (database, blockchain, email, etc.)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..." # or pk_live_... for production

# Backend server URL (deployed on Cloudflare Workers)  
NEXT_PUBLIC_BACKEND_URL="https://alomnify-api.alomnify.workers.dev"